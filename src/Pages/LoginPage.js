import React from 'react';
import LoginComp from "../Components/LoginComp";

const LoginPage = () => {
    return (
        <div>
            <LoginComp/>
        </div>
    );
};

export default LoginPage;